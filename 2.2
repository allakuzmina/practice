#include <cstdlib>
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  setlocale (0,"");
    float z,t,a=-0.6,b=5.3;
    if (a<b) {
              z= sqrt(abs(a*a)-abs(b*b));}
    else{
         z=1-2*cos(a)*sin(b);}
    if (z<b) {
    t=pow(z+a*a*b,1/3);}
    else if (z==b) {
      t=1-log(z)+cos(a*a*b);}
    else { 
      t=1/(cos(2*a));} 
    cout << z<<" "<<t << endl;
    return 0;
  }
