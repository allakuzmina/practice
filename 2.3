#include <cstdlib>
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
  setlocale (0,"");
    float y,t,a=4.6,b=1.5;
    if (a<=b) {
              y= ((a-b)/(a+b))*((a+b)/(a*a-a*b+b*b));}
    else{
   y=a+log(b*b);}
    if (y>b) {
    t=pow(y*sin(a),1/3)+1/(sqrt(y*cos(b)));}
    else if (y==b) {
      t=pow(sin(y),2)+1/(tan(a-b));}
    else { 
      t=(2*y+sqrt(pow(y,2)-a))/(2*b-sqrt(a*a-y));} 
    cout << y<<" "<<t << endl;
    return 0;
  }
